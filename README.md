# Supply-Chain-Logistic-Analytical-Dashboard
This project demonstrates how raw operational data from multiple Excel files can be transformed into structured, decision-ready insights using Power BI. The objective was not just to create visualizations, but to apply an analytical approach that converts scattered data into meaningful business intelligence.
